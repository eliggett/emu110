# Build products.  Regenerate with tools/build_core.sh and make.
build/
generated/
bin/
# The Windows cross build keeps its own, for the reason in the Makefile's `win` target.
build-win/
bin-win/
*.o
*.d
